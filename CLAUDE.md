# CLAUDE.md

# Claude Code Working Rules
Project: 프리랜서 미용사 월급/정산 계산기

## 1. 시작 시 반드시 할 일

작업 시작 전에 아래 순서로 확인한다.

1. MASTER.md 읽기
2. PROJECT_STATE.md 읽기
3. 현재 git 상태 확인
   - git status
   - git diff
   - 최근 commit 확인
4. 현재 코드 구조 파악
5. PROJECT_STATE.md의 NEXT 항목 기준으로 작업 시작

사용자에게 이미 확정된 내용을 다시 질문하지 않는다.

---

## 2. 최우선 목표

이 프로젝트의 최우선 목표는:

> 최대한 빠르게 실제 사용 가능한 v0.1을 출시하는 것.

다음 행동을 피한다.

- 불필요한 추상화
- 미래 기능을 위한 과도한 구조
- 필요하지 않은 서버 구축
- 필요하지 않은 인증
- 필요하지 않은 상태관리 라이브러리
- 디자인 시스템 과설계
- 복잡한 패턴 도입
- 사용자가 요청하지 않은 기능 추가

"좋은 구조"보다 "작동하는 작은 MVP"를 우선한다.
단, 정산 계산 정확성과 데이터 무결성은 타협하지 않는다.

---

## 3. 확정사항 보호

MASTER.md의 `LOCKED DECISIONS`는 사용자 명시적 변경 요청 없이는 바꾸지 않는다.

특히 다음을 임의 변경하지 않는다.

- 헤어 디자이너 개인 정산 앱
- 웹/PWA 우선
- 서버/회원가입 없이 시작
- 거래 한 건씩 누적
- 월정산 상단 요약 + 하단 달력
- 달력 날짜별 매출/정산 표시
- 날짜 클릭 → 일별 상세
- 정액권 별도 원장
- 정액권 판매즉시 반영형 + 사용시 반영형 고려
- 과거 거래 snapshot 보존
- 빠른 MVP 우선

---

## 4. 기술 기본값

별도 변경 지시가 없으면:

- Next.js
- TypeScript
- Tailwind CSS
- 모바일 퍼스트
- PWA 가능한 구조
- 초기 로컬 저장
- 서버 없음

새 라이브러리를 추가하기 전에:
"이 라이브러리가 v0.1 출시 속도를 실제로 높이는가?"
를 먼저 판단한다.

아니면 추가하지 않는다.

---

## 5. 코드 작성 규칙

### 일반
- TypeScript strict-friendly
- 타입을 명확히 정의
- 계산 로직과 UI를 분리
- 함수는 작고 테스트 가능하게 유지
- 의미 없는 generic abstraction 금지
- 한 기능을 위해 지나치게 많은 파일 생성 금지

### 정산 엔진
정산 계산 로직은 UI 컴포넌트 내부에 직접 작성하지 않는다.

예:
- lib/settlement/*
- domain/settlement/*
등 한 곳에 모은다.

정산 계산은 입력 → 결과가 명확한 순수 함수 위주로 만든다.

---

## 6. 정산 데이터 핵심 규칙

### 과거 거래 snapshot
거래 저장 시 당시 적용된 값을 보존한다.

최소:
- 적용 인센티브율
- VAT 처리 결과
- 카드/결제수수료
- 재료비
- 정산 기준금액
- 최종 예상 정산액

사용자가 나중에 설정을 바꿔도
기존 거래의 저장된 정산결과를 임의 변경하지 않는다.

### 재계산
과거 거래를 새 설정으로 재계산하는 기능은
사용자가 명시적으로 요청하기 전까지 만들지 않는다.

---

## 7. 정액권 규칙

정액권을 일반 paymentType으로 단순 처리하지 않는다.

반드시 별도 구조:
- PrepaidPass
- PrepaidEvent

이벤트 최소 타입:
- PURCHASE
- USE
- REFUND
- TRANSFER
- ADJUSTMENT

잔액은 이벤트 이력으로 검증 가능해야 한다.

정액권 환불/타디자이너 사용 시
기존 과거 기록을 덮어쓰기보다 조정 이벤트를 추가하는 방향을 우선한다.

정액권 보너스:
- paidAmount
- creditAmount
를 분리한다.

---

## 8. 월정산 화면 규칙

월정산은 제품 핵심 화면이다.

### 상단
최소:
- 총매출
- 예상 정산액
- 실제 지급액
- 차이
- 정액권 관련 조정

### 하단
월간 달력.

각 날짜:
- 날짜
- 하루 총매출
- 하루 정산금액

날짜 클릭:
- 해당 날짜 상세 화면으로 이동

달력 셀에 너무 많은 정보를 넣지 않는다.
모바일 가독성을 우선한다.

---

## 9. UX 원칙

헤어 디자이너가 영업 중에도 빠르게 입력할 수 있어야 한다.

거래 등록:
- 최대한 적은 탭
- 큰 금액 입력창
- 자주 쓰는 고객유형/시술/결제수단 버튼화
- 저장 전 예상 정산금액 표시 가능하면 구현

사용자가 정산 공식을 이해하지 못해도 사용할 수 있어야 한다.

설정 화면은:
- 기본 설정
- 고급 설정
으로 분리하는 방향을 우선한다.

---

## 10. 작업 방식

한 번에 큰 기능을 여러 개 만들지 않는다.

권장 단위:
1. 데이터 타입
2. 계산 엔진
3. 저장
4. UI 연결
5. 테스트
6. 상태 문서 업데이트

기능 하나가 동작하기 전에 다음 기능으로 넘어가지 않는다.

---

## 11. 오류 대응

오류 발생 시:

1. 원인 확인
2. 기존 정상 동작 범위 확인
3. 최소 변경으로 수정
4. 관련 기능 재테스트
5. 불필요한 리팩터링 금지

같은 오류를 반복해서 못 잡으면
문제를 작은 재현 단위로 줄인다.

기존 정상 기능을 통째로 재작성하지 않는다.

---

## 12. 테스트 우선순위

특히 아래 계산 케이스는 반드시 테스트한다.

### 일반
- 기본 인센티브
- 신규/재방문/지정 서로 다른 비율
- VAT 없음
- 10% 단순 차감
- 공급가액 환산
- 카드수수료
- 재료비 %
- 재료비 고정값
- 3.3% ON/OFF

### 정액권
- 판매 즉시 반영
- 사용 시 반영
- 부분 사용
- 타 디자이너 사용
- 부분 환불
- 전액 환불
- 보너스 정액권
- 잔액 0
- 잘못된 초과 사용 방지

### 월
- 월 변경
- 월 경계 날짜
- 마이너스 거래
- 거래 수정
- 거래 삭제/조정
- 실제 지급액 입력

---

## 13. 파일/문서 관리

프로젝트 문서는 기본적으로 3개만 유지한다.

- MASTER.md
- PROJECT_STATE.md
- CLAUDE.md

새 문서는 꼭 필요할 때만 생성한다.

의미 있는 기능이 끝나면 PROJECT_STATE.md를 실제 상태에 맞게 업데이트한다.

---

## 14. Git 규칙

의미 있는 기능 단위 완료 후:

1. 앱 실행/테스트
2. PROJECT_STATE.md 업데이트
3. git status
4. git diff 검토
5. commit
6. push

임의로 대규모 파일 삭제/덮어쓰기 금지.

커밋 예시:
- feat: add settlement rule engine
- feat: add transaction entry flow
- feat: add monthly settlement calendar
- feat: add prepaid pass ledger
- fix: preserve settlement snapshot after settings change

---

## 15. Claude의 판단 기준

선택지가 여러 개면 아래 우선순위로 결정한다.

1. 사용자의 현재 명시적 요청
2. MASTER.md LOCKED DECISIONS
3. PROJECT_STATE.md 현재 상태
4. 빠른 MVP 출시
5. 구현 단순성
6. 유지보수성
7. 확장성

확장성을 위해 출시를 늦추지 않는다.

---

## 16. 하지 말아야 할 것

사용자 요청 없이 아래 기능을 추가하지 않는다.

- Supabase
- Firebase
- 로그인
- 회원가입
- 관리자 페이지
- POS 연동
- 예약 시스템
- 고객 CRM
- 채팅
- 커뮤니티
- AI
- 과도한 통계
- 복잡한 차트
- 구독 결제
- 다중 매장
- 다중 직원

아이디어가 떠올라도 구현하지 말고
필요하면 PROJECT_STATE.md의 "Later" 후보로만 기록한다.

---

## 17. 완료의 정의

코드를 작성한 것만으로 완료가 아니다.

완료 조건:
- 실제 실행 가능
- 해당 기능 테스트 완료
- 기존 기능이 깨지지 않음
- 모바일 화면 확인 가능
- PROJECT_STATE.md 업데이트 완료

항상 "다음 사람이 이어서 바로 작업할 수 있는 상태"로 종료한다.
